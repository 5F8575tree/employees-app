import React from "react";
import Hero from "./Hero";
import LoginForm from "./LoginForm";

const LoginPage = () => {
  return (
    <div>
      <Hero />
      <LoginForm />
    </div>
  );
};

export default LoginPage;
